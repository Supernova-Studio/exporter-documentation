{* Fetch rich from injection context, expected type 'Unordered_list' *}
{[ const listItem = context /]}

{* Create opening block, if it opens list chain *}
{[ log listItem /]}
{[ if listItem.beginsTypeChain ]}
<ol>
{[/]}
<li>{[ inject "page_block_rich_text" context listItem.text /]}</li>

{* Create closing block, if it opens list chain *}
{[ if listItem.endsTypeChain ]}
</ol>
{[/]}