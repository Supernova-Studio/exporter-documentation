{* Fetch block from injection context, expected type 'Heading' *}
{[ const block = context /]}

{* Create image that can't be interacted with, or ignore it if url is not set *}
{[ if block.url.isString() ]}
<img src="{{ block.url }}" class="img-fluid" alt="Responsive image">
{[/]}